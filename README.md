# Puzzle

HTML puzzle game

Create a matrix (c,r)
set size of each item in matrix
attach image to each item in matrix (use offset of image based on column/row) creating a piece

randomize pieces

display matrix in dropzone area
set each piece as "droppable" = if piece not in position

if out of range - don't drop
